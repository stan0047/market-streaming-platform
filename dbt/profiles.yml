market_streaming:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      dbname: market_db
      user: market_user
      password: market_pass
      schema: public
      threads: 1
